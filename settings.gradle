pluginManagement {
    repositories {
        maven { url "https://plugins.gradle.org/m2/" }
//        mavenCentral()
        gradlePluginPortal()
        mavenLocal()
    }
    plugins {
        id "${quarkusPluginId}" version "${quarkusPluginVersion}"
    }
}
rootProject.name='depinalliance-api'
